query MorphoBorrowRates($first: Int, $side: InterestRateSide!) {
  interestRates(first: $first, where: { side: $side }) {
    id
    rate
    side
    market {
      id
      name
      inputToken {
        symbol
      }
    }
  }
}
